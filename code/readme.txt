ALEJANDRO CIRUGEDA
EDAMI PROYECT - KMEANS WITH PARALLEKL IMPLEMENTATION

The main.py is in the src folder

In the output folder you can find the output of each dataset

In the input foldel you can find the different datasets:
- Iris
- Articial dataset
- Power consuption

You can select the dataset that is used in the execution of the algorithm just before the 
main function in main.py

for the Iris and artificial dataset the ',' separator is used
and for the power consuption the ';' is used

also is neded too select with points to read from the file in the read_file function
